receivers:
  otlp:
    protocols:
      grpc:
      http

exporters:
  otlphttp/tempo:
    endpoint: http://tempo:3200
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp/tempo]
